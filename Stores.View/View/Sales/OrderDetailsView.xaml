<UserControl x:Class="Stores.View.View.Sales.OrderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="5">
            <Label FontWeight="Bold" Content="Order Details"></Label>
            <ListView ItemsSource="{Binding OrderItems}" Height="150">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Product.ProductName, StringFormat='Name: {0}'}"></TextBlock>
                            <TextBlock Text="{Binding ListPrice, StringFormat='Price: {0}'}"></TextBlock>
                            <TextBlock Text="{Binding Quantity, StringFormat='Quantity: {0}'}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
